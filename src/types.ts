export interface CarrinhoItemType {
  produtoId: number;
  nome: string;
  preco: string;
  quantidade: number;
}
export interface ProdutoType {
  id: number;
  nome: string;
  preco: string;
  imagem: string;
  estoque: number;
}
